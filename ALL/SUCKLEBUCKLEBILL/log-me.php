<?php
include "functfc.php";
$search = getValue('username',null);

//$userfile = fopen("$file.txt", "w+") or die("Unable to open file!");
$txt = " \$self = array(
    username => '$username';
    )"
//fwrite($userfile, $data);
//fclose($userfile);
?>